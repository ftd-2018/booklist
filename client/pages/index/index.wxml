<!--index.wxml-->
<view class="container">
  <ftdNav></ftdNav>
  <view class="serach">
    <navigator url="/pages/search/index">请输入专业</navigator>
  </view>
  <view class="booklist">
    <ftdBL
      wx:for="{{courseList}}"
      title="{{item.title}}"
      isCollect="{{item.isCollect}}"
      courseID="{{item.id}}"
    ></ftdBL>
  </view>
</view>
