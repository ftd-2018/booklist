<!--index.wxml-->
<view class="container">
  <ftdNav></ftdNav>
  <view class="serach">
    <navigator url="/pages/search/index">请输入专业</navigator>
  </view>
  <view class="booklist">
    <ftdBL
      wx:for="{{majorList}}"
      name="{{item.name}}"
      isCollect="{{item.isCollect}}"
      majorID="{{item.majorID}}"
    ></ftdBL>
  </view>
</view>
